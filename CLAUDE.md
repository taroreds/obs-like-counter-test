@AGENTS.md

## 文書の役割分担

| ファイル | 内容 | 更新の仕方 |
| --- | --- | --- |
| `docs/decisions.md` | なぜその判断をしたか。理由と却下した案 | 追記のみ。既存項目は §13 の手順を経ずに書き換えない |
| `docs/architecture.md` | 現在の構造と責務 | 実装に追随して書き換える |
| `README.md` | 利用者向けの手順と注意 | 利用者から見える挙動が変わった時 |
| `AGENTS.md` | エージェントが守る規約 | 規約そのものが変わった時のみ |

実装を変更した場合は、`docs/architecture.md` の該当箇所を同じ変更の中で更新する。

## Claude Code への指示

- 明示的な指示がない限りコミットしない。
- 編集前に変更計画と対象ファイルを提示し、承認を得てから着手する。
- 作業範囲を依頼された内容に限定する。関連して気づいた問題は、修正せず報告する。
- 実機で確認していないOBS上の動作を、確認済みと表現しない。
- 既存の改行コード（CRLF）を維持する。ファイル全体をLFへ変換しない。
- 新しい判断を要する論点に気づいた場合は、`docs/decisions.md` への追加候補として報告する。自分で追記しない。
