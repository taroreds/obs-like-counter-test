# AGENTS.md

## プロジェクトの目的

高評価目標などの進捗を表示する、軽量なOBSブラウザソース用ウィジェットです。

利用者がZIPファイルを展開し、OBSのブラウザソースで `index.html` を指定すれば動作することを目標とします。

判断の理由と却下した案は `docs/decisions.md`、現在の構造と責務は `docs/architecture.md` に記録します。このファイルはエージェントが守るべき規約だけを扱います。

## 必須制約

- OBSブラウザソースからローカルファイルとして動作する互換性を維持する。
- 明示的な承認なしにビルドツールを導入しない。
- 明示的な承認なしに外部の実行時依存関係を導入しない。
- 初期版ではWebサーバーを必要としない。
- 明確な理由なく、1ファイルに複数の責務を持たせない。
- 描画処理と状態変更処理を分離する。
- 外部から与えられる値はすべて正規化・検証する。
- 不正な設定値によって、画面全体を空白または利用不能にしない。
- 進捗表示値は有効な範囲内に制限する。
- 利用者が編集する設定項目は限定し、文書化する。
- 明示的な変更指示がない限り、既存のテーマ、配置、サイズ設定を維持する。
- 要件を満たす最小限の変更を優先する。
- 現時点で不要な抽象化や将来機能の先行実装を避ける。
- 特定のフレームワーク知識がなくても読めるコードを維持する。
- すべての入力アダプターは、同一の状態管理インターフェースを通じて状態を変更する。
- 入力アダプター自身に独立した描画処理を持たせない。
- 現行ウィジェット本体では外部APIを使用しない。API連携を検討する場合は、本体へ組み込む前に独立したスパイクを行う（`docs/decisions.md` D-034）。

## エラー処理方針

- 設定値が欠けている場合は、文書化された初期値を使う。
- 不正な数値には安全な代替値を使う。
- 負数のカウンター値は表示しない。
- 目標値が0以下でも、ゼロ除算を発生させない。
- 未定義のテーマ、配置、サイズには初期値を使う。
- 回復可能なエラーによって、他の表示処理を停止させない。

## コーディング規約

- HTML、CSS、素のJavaScriptを使用する。
- JavaScriptでは、モジュールまたは即時実行関数内で `"use strict"` を使用する。
- 関数名と変数名は役割が分かるものにする。
- 関数は小さく、責務を限定する。
- グローバル変数は、明示的な設定名前空間と意図的に公開するアプリケーションインターフェースに限定する。
- コードから意図が明らかな箇所には不要なコメントを付けない。
- すべてのファイルをUTF-8で保存する。

## 変更時の手順

編集前に次を行う。

1. 現在の実装を確認する。
2. 設計案と変更対象ファイルを示す。
3. 互換性上のリスクを特定する。
4. 変更範囲を依頼された作業に限定する。

編集後に次を行う。

1. 通常のブラウザでローカルファイルとして読み込めることを確認する。
2. OBSブラウザソースで読み込めることを確認する。
3. 正常な設定値と不正な設定値をテストする。
4. 変更したファイルと挙動を要約する。
5. 実際に確認していないOBS上の動作を、確認済みと表現しない。
